class Solution {
    public int countOdds(int low, int high) {
       return ((high + 1) / 2) - (low / 2);
       // odd count till high - odd count till low
       //odd count = (n+1)/2
    }
}
/* broute force approach:-
 for(int i = low ; i<=high ; i++ )
        {
            if(i%2!=0){
                count++;
            }
        }
        return count; */
